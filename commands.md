<!-- docker build -t custom-postgres -f docker/Dockerfile.postgres .
docker-compose down
docker-compose up --build -->

<!-- for the database inside the docker container -->

<!-- docker exec -it postgres_{username} bash
psql -U {database_name} # database_name = username
\dt
... -->
